import { CreateRouteReviewRequest } from './create-route-review-request';

describe('CreateRouteReviewRequest', () => {
  it('should create an instance', () => {
    expect(new CreateRouteReviewRequest()).toBeTruthy();
  });
});
