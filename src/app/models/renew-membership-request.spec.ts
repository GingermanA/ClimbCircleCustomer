import { RenewMembershipRequest } from './renew-membership-request';

describe('RenewMembershipRequest', () => {
  it('should create an instance', () => {
    expect(new RenewMembershipRequest()).toBeTruthy();
  });
});
